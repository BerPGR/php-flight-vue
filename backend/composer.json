{
    "name": "fab/projeto-teste",
    "type": "project",
    "require": {
        "flightphp/core": "^3.17",
        "vlucas/phpdotenv": "^5.6"
    },
    "autoload": {
        "psr-4": {
            "App\\": "src/"
        }
    },

    "scripts": {
        "start": "php -d display_errors=1 -d error_reporting=E_ALL -S localhost:8000 -t public 2>&1 | tee backend/var/php-server.log"
    }
}
